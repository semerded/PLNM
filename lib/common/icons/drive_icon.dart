import 'package:flutter/material.dart';

class DriveIcon extends StatelessWidget {
  const DriveIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/images/drive_logo.png", width: 40, height: 40,);
  }
}