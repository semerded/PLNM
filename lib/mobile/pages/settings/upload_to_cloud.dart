import 'package:flutter/material.dart';

class UploadToCloud extends StatelessWidget {
  const UploadToCloud({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
